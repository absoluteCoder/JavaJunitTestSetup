package com.company;

public class Calculator {
    int add(String arg){
        return 0;
        // first one is to remove space
        //
        // 2: is to split string by comma
        // 2: r

//        if(arg == null || arg.isEmpty()){
//            return 0;
//        }
//        String numbers[] = arg.split(",");
//        int res = 0;
//        for(String number: numbers) res += Integer.parseInt(number);
//        return res;
    }
}
